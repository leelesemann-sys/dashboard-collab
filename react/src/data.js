// ── Shared Mock Data ─────────────────────────────────────────
import raw from "../../shared/mock-data.json";

export const MONTHLY = raw.monthly;
export const REGIONS = raw.regions;
export const COMPETITORS = raw.competitors;
export const KPIS = raw.kpis;
